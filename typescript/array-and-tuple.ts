let arrOfNumbers: number[] = [1,2,3]

// arrOfNumbers.push('123')

function test(){
  // 类数组
  console.log(arguments)
  // let arr: any[] = arguments
  // let htmlCollection: HTMLCollection
}

// 元组 合并不同类型的对象
let user: [string, number] = ['catcian', 30]
user.push(true)